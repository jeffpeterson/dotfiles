#!/usr/bin/env bash

  # --html-extension \
  # --adjust-extension \
wget \
  --no-verbose \
  --recursive \
  --page-requisites \
  --convert-links \
  --restrict-file-names=unix \
  --no-parent \
    ${@:1}
