#!/usr/bin/env bash

  # --html-extension \
wget \
  --no-verbose \
  --recursive \
  --no-clobber \
  --page-requisites \
  --adjust-extension \
  --convert-links \
  --restrict-file-names=unix \
  --no-parent \
    ${@:1}
